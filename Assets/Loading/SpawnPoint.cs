using UnityEngine;

/// <summary>
/// Este script é apenas um marcador. 
/// Ele dá um ID (nome) para um ponto de spawn na cena.
/// </summary>
public class SpawnPoint : MonoBehaviour
{
    // O nome deste objeto (SpawnPoint) no Inspector
    // será o ID que o SceneTrigger vai procurar.
}